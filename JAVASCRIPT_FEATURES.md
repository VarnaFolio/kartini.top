# JavaScript Функционалности - Картини.top

## 📋 Съдържание
1. [Общ преглед](#общ-преглед)
2. [Инсталирани функции](#инсталирани-функции)
3. [Как работят функциите](#как-работят-функциите)
4. [Примери за използване](#примери-за-използване)
5. [Персонализация](#персонализация)

---

## 🎯 Общ преглед

Всички интерактивни JavaScript функционалности са добавени към сайта. Файлът `script.js` е готов и свързан с всички страници.

### Файлова структура:
```
kartini.top/
├── script.js                    ← Главен JavaScript файл
├── styles.css                   ← CSS с нови стилове за JS функции
├── pages/
│   ├── categories.html          ← Включва script.js + бутон за тема
│   ├── abstraktni-kartini.html  ← Включва script.js + модал + бутон за тема
│   ├── minimalisticni-kartini.html
│   ├── prirodni-motivi.html
│   ├── silueti-portreti.html
│   ├── geometricni-boho.html
│   └── mnogochastni-kartini.html
```

---

## ✨ Инсталирани функции

### 1. **Тъмна/Светла Тема (Theme Toggle)** 🌙☀️
- **Бутон**: Фиксиран в долния десен ъгъл на всяка страница
- **Иконка**: 🌙 за светла тема, ☀️ за тъмна тема
- **Памет**: Запазва избора в `localStorage`
- **Цветова схема**: Автоматично сменя цветовете на карти, текст, фонове

#### Как работи:
```javascript
// Бутонът автоматично се инициализира при зареждане
// Потребителят кликва → темата се сменя → избор се запазва
document.getElementById('theme-toggle').addEventListener('click', toggleTheme);
```

---

### 2. **Система за Любими (Wishlist)** ❤️
- **Бутон**: Бутон със сърце при всеки продукт
- **Брояч**: Показва брой любими продукти в навигацията
- **Памет**: Запазва любими в `localStorage`
- **Визуален фийдбек**: Анимация при добавяне/премахване

#### Как работи:
```javascript
// Всеки бутон с клас .btn-wishlist е активен
// Кликване → продукт се добавя/премахва от любими
// data-product-id идентифицира уникален продукт
```

#### Пример HTML:
```html
<button class="btn-wishlist" data-product-id="1">♡</button>
```

---

### 3. **Филтри и Сортиране** 🔍
- **Филтри**: По цвят, стил, завършек
- **Сортиране**: По цена, популярност, дата
- **Динамично**: Скрива/показва продукти в реално време

#### Как работи:
```javascript
// Филтрите използват data атрибути на продуктите
// data-color="червена" → филтър "Червени"
// data-style="минималистично" → филтър "Минималистични"
```

#### Пример HTML за продукт:
```html
<article class="product-card" 
    data-color="многоцветна" 
    data-style="абстрактно" 
    data-finish="гланцово">
    <!-- Съдържание -->
</article>
```

#### Пример за бутон филтър:
```html
<button class="filter-btn" data-filter-type="color" data-filter-value="червена">
    Червени
</button>
```

---

### 4. **Бърз Преглед (Quick View Modal)** 👁️
- **Активация**: Кликване на "Бърз преглед" при hover върху продукт
- **Съдържание**: Голямо изображение, детайли, цена, спецификации
- **Действия**: Директно добавяне в любими или поръчка

#### Как работи:
```javascript
// Бутонът "Бърз преглед" съдържа data атрибути с информация
// Кликване → модалът се попълва с данни → показва се
```

#### Пример HTML:
```html
<button class="btn-quick-view" 
    data-title="Абстрактна композиция #1"
    data-price="89.00"
    data-description="Ярки цветни форми..."
    data-size="40x60 см"
    data-material="Канава"
    data-frame="Черна дървена"
    data-finish="Гланцово">
    Бърз преглед
</button>
```

---

### 5. **Lazy Loading (Мързеливо Зареждане)** 🖼️
- **Оптимизация**: Изображенията се зареждат само когато са видими
- **Placeholder**: SVG плейсхолдъри с shimmer ефект
- **API**: IntersectionObserver за висока производителност

#### Как работи (когато добавите реални изображения):
```html
<!-- Вместо src="" използвайте data-src="" -->
<img data-src="путь/към/изображение.jpg" alt="Product">
```

Скриптът автоматично ще зареди изображението когато стане видимо.

---

### 6. **Нотификации (Notifications)** 🔔
- **Показват се при**: Добавяне/премахване от любими
- **Типове**: Success (зелен), Error (червен), Info (златен)
- **Автоматично затваряне**: След 3 секунди
- **Позиция**: Долен десен ъгъл

---

### 7. **Допълнителни функции**
- ✅ **Smooth Scroll** - Плавно скролване при кликване на връзки
- ✅ **Mobile Menu** - Адаптивно меню за мобилни устройства
- ✅ **Gallery Filters** - Филтри в галерия секцията

---

## 🔧 Как работят функциите

### Автоматична Инициализация
Всички функции се инициализират автоматично при зареждане на страницата:

```javascript
document.addEventListener('DOMContentLoaded', function() {
    initTheme();          // Зарежда запазената тема
    initFilters();        // Активира филтрите
    initLazyLoading();    // Стартира lazy loading
    initSmoothScroll();   // Включва плавно скролване
    initMobileMenu();     // Настройва мобилно меню
    initGalleryFilters(); // Активира галерия филтри
});
```

---

## 💡 Примери за използване

### Добавяне на нов продукт с всички функции:

```html
<article class="product-card" 
    data-color="синя" 
    data-style="минималистично" 
    data-finish="матово">
    <div class="product-image">
        <div class="product-badge">Ново</div>
        <div class="product-image-placeholder">
            <!-- SVG Placeholder -->
        </div>
        <div class="product-overlay">
            <button class="btn-quick-view" 
                data-title="Морски пейзаж"
                data-price="79.00"
                data-description="Минималистичен морски пейзаж в сини тонове"
                data-size="30x40 см"
                data-material="Канава"
                data-frame="Бяла дървена"
                data-finish="Матово">
                Бърз преглед
            </button>
        </div>
    </div>
    <div class="product-content">
        <h3 class="product-title">Морски пейзаж</h3>
        <p class="product-description">Минималистичен морски пейзаж</p>
        <div class="product-options">
            <span class="product-size">30x40 см</span>
            <span class="product-finish">Матово</span>
        </div>
        <div class="product-footer">
            <a href="tel:0883341795" class="btn-primary btn-small">Поръчай</a>
            <button class="btn-wishlist" data-product-id="7">♡</button>
        </div>
    </div>
</article>
```

---

## 🎨 Персонализация

### Промяна на цветове на тъмната тема:
Редактирайте `styles.css`, секция "Dark Theme":

```css
body.dark-theme {
    --text-color: #f8f8f8;      /* Цвят на текста */
    --bg-color: #1a1a1a;        /* Цвят на фона */
    --card-bg: #2a2a2a;         /* Цвят на картите */
    --border-color: #444;       /* Цвят на рамките */
}
```

### Промяна на позиция на бутона за тема:
```css
#theme-toggle {
    bottom: 30px;  /* Разстояние отдолу */
    right: 30px;   /* Разстояние отдясно */
}
```

### Промяна на време за нотификации:
Редактирайте `script.js`, функция `showNotification`:

```javascript
setTimeout(() => {
    notification.classList.remove('show');
    setTimeout(() => notification.remove(), 300);
}, 3000);  // ← Промени 3000 (3 секунди) на желаното време
```

---

## 📱 Responsive Design

Всички функции са адаптивни и работят на:
- 📱 **Мобилни** (480px и по-малко)
- 📱 **Таблети** (768px)
- 💻 **Десктоп** (1024px+)

---

## 🐛 Отстраняване на проблеми

### Ако нещо не работи:

1. **Проверете конзолата** (F12 → Console)
2. **Уверете се, че `script.js` е зареден**:
   ```html
   <script src="../script.js"></script>
   ```
3. **Проверете data атрибутите** на продуктите
4. **Изчистете кеша** на браузъра (Ctrl + Shift + R)

---

## 📝 Бележки

- ✅ Всички функции използват **localStorage** за запазване на данни
- ✅ **Без външни библиотеки** - само чист JavaScript
- ✅ **SEO оптимизирано** - не влияе негативно на индексирането
- ✅ **Бърза производителност** - минимално натоварване

---

## 🎉 Готово!

Сайтът е напълно функционален с всички модерни интерактивни функции. Просто отворете страниците в браузър и тествайте!

### Тестване:
1. Отворете `pages/abstraktni-kartini.html`
2. Кликнете бутона за тема (долу-дясно) 🌙
3. Натиснете "Бърз преглед" на продукт 👁️
4. Добавете продукт в любими ❤️
5. Тествайте филтрите 🔍

**Успех!** 🚀
